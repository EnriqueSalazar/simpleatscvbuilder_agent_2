{
  "name": "simpleatscvbuilder_agent_2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "pnpm run start --filter ./client ./server",
    "dev": "concurrently \"pnpm run dev:client\" \"pnpm run dev:server\"",
    "dev:client": "pnpm --filter ./client dev",
    "dev:server": "pnpm --filter ./server dev"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
